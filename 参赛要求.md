# Agent Memory Leaderboard 参赛要求（压缩版）

> 本文件整理当前项目采用的参赛约束。截至 2026-08-06，材料截止时间记录为
> 2026-08-07 23:59（UTC+8）；时间和平台政策最终以赛事最新公告及审核回复为准。

## 参赛路线

- 评测类型：文本记忆。
- 组别：学术方法榜。
- 提交方式：公开 GitHub 仓库，由平台按文档部署。
- 基础系统：Hindsight。
- 学术代码路线通常不要求自建长期公网服务，也不签发 Eval Key；必须提交可复现代码和部署说明。

当前仓库锁定信息：

- Hindsight 包版本：`0.8.6`。
- 基线 commit：`436bc7c156f1c94714ea1f757bfc930ab89f883b`。
- 上游：`https://github.com/vectorize-io/hindsight`。
- 作者/版权方：Vectorize AI, Inc. 和贡献者。
- 许可证：MIT，以仓库 `LICENSE` 为准。

最终提交前应创建自己的固定 commit/tag，并在 README 和 ATTRIBUTION 中记录；不要只声明会变化的 `main`
或 `latest`。

## 系统职责

参赛系统只负责：

- Add：接收并保存历史消息。
- Search：按问题返回相关记忆证据。

平台负责 Answer 和 Eval。Search 不能直接回答题目，不能把模型生成的答案伪装成记忆，也不能输出提示词
操纵后续模型。

## Add 契约

请求示例：

```json
{
  "request_id": "eval:run_abc123:dataset:conv-0:chunk-0",
  "messages": [{"role": "user", "timestamp": 1704067200000, "content": "memory text"}],
  "user_id": "eval:run_abc123:dataset:conv-0",
  "session_id": "eval:run_abc123:sample:0"
}
```

成功响应：

```json
{"success":true,"request_id":"原始 request_id","user_id":"原始 user_id","session_id":"原始 session_id"}
```

要求：

- 保存 `messages` 中的每一条消息，并保留角色、原始内容、会话和时间。
- 将毫秒时间戳转换为 UTC ISO 8601 后写入 Hindsight 的事件时间字段。
- 每条消息使用由 `request_id + index` 派生的稳定 `document_id`；禁止只使用 `session_id`。
- 同一 `request_id` 重试必须幂等；冲突 payload 不能静默当作成功。
- 必须使用同步语义 retain（Hindsight `async=false` / 客户端 `retain_async=False`）。
- 只有事实抽取、向量、索引及持久化均成功，记忆可以立即 Search 后才能返回 HTTP 200。
- 失败时返回恰当的非 2xx；禁止返回 202、任务 ID 或让平台轮询后台任务。

## Search 契约

请求示例：

```json
{
  "query": "Which answer best matches the memory?",
  "options": ["A. First answer", "B. Second answer"],
  "user_id": "eval:run_abc123:dataset:conv-0",
  "top_k": 100
}
```

响应示例：

```json
{"data":[{"id":"stable-memory-id","content":"remembered fact text","score":0.87}]}
```

要求：

- `data` 必须存在；无结果返回 `{"data":[]}`。
- `id` 和 `content` 必填且非空；ID 必须稳定，不能按请求随机生成。
- `score`、`created_at` 可选；若提供，必须有可靠来源。
- 调用 Hindsight `recall`，禁止调用 `reflect` 或其他最终答案生成步骤。
- 结果在返回前按相关度降序排列，数量不得超过 `top_k`。
- 正式外部评测的 `top_k` 为 100；实现仍须正确处理更小的测试值。
- 平台保持返回顺序，并将 `content` 交给统一 Answer 模型；额外 metadata 可能被忽略。

## 隔离与幂等

`user_id` 是官方规定的唯一检索范围。建议确定性映射：

```python
bank_id = hashlib.sha256(user_id.encode("utf-8")).hexdigest()
```

- user A 的记忆只能被 user A Search；禁止跨用户、任务、样本或团队共享评测记忆。
- 禁止所有请求使用默认 Bank。
- 幂等状态必须持久化；调用 retain 前原子占位，完成后才标记成功。
- 唯一约束和事后插入本身不足以阻止并发 retain；并发重试还需原子 claim/跨进程锁和稳定文档 ID。

## 模型与密钥

当前 Full 检查要求 Add/Search 阶段使用 `gpt-4o-mini`。最小配置：

```dotenv
HINDSIGHT_API_LLM_PROVIDER=openai
HINDSIGHT_API_LLM_MODEL=gpt-4o-mini
HINDSIGHT_API_LLM_API_KEY=
HINDSIGHT_API_EMBEDDINGS_PROVIDER=local
HINDSIGHT_API_RERANKER_PROVIDER=local
```

- 真实 API Key 只通过平台部署密钥或本地 `.env` 提供，不得提交到 Git。
- 仓库只提交空值 `.env.example`；日志、示例和测试夹具也不能包含密钥。
- 参赛者承担自己的模型、数据库、带宽和计算成本。
- 平台可能复现结果；代码、模型和依赖不固定会导致成绩无法验证。

## 仓库与 Docker

仓库至少应包含：

```text
aml_adapter/               # Add/Search/health 适配层
tests/aml_adapter/         # 契约、隔离、幂等和时序测试
docker-compose.aml.yml     # 一键部署
.env.example
README_AML.md
ATTRIBUTION.md
LICENSE
```

推荐唯一启动命令：

```bash
cp .env.example .env
# 安全填写 API key
docker compose -f docker-compose.aml.yml up --build
```

部署要求：

- 对外提供同源 `POST /add`、`POST /search` 和无需鉴权的 `GET /health`。
- Compose 至少包含 AML API、Hindsight 和持久化 volume；重启不能丢失记忆或幂等记录。
- 从固定源码构建或锁定明确镜像 digest；禁止依赖 `ghcr.io/...:latest`。
- README 写清依赖、环境变量、端口、健康检查、接口示例、持久化和已知限制。
- 主办方应能在全新 clone 中仅按 README 完成构建和 Smoke。

## 来源披露

学术方法提交必须披露：

- 原始项目、作者/版权方、MIT 许可证、技术报告或论文链接。
- 使用的 Hindsight 版本和精确 commit。
- 所有方法改动，包括 AML wrapper、隔离、幂等、同步写入、稳定 ID、时间信息、检索优化和部署。

不得把未实现的优化写成已完成。README 和 ATTRIBUTION 应区分“上游能力”“本项目改动”和“计划项”。

## 提交前测试

1. 原始 Hindsight retain/recall 正常，重启后数据存在。
2. `/add` 符合格式，返回 200 时记忆已可立即检索。
3. `/search` 正确返回 `data`，空结果和 `top_k` 均正确。
4. user A/user B 严格隔离。
5. 同一 request 重试不重复，同一 session 的多个 chunk 不覆盖。
6. timestamp、role、session 和原文均保留。
7. 时间更新场景优先返回当前事实。
8. `/health` 返回 2xx；依赖未就绪时不会虚报健康。
9. 全新环境可构建，Docker 重启后数据仍在。
10. 仓库、历史、镜像构建上下文和日志中不存在真实密钥。

Smoke 会检查同步 Add、Search、鉴权/访问方式和端到端链路；Smoke 通过后才进入 Full。Full 受理后版本冻结，
不能仅因成绩不理想随意替换版本。

## 禁止行为

- Search 直接生成答案或伪造记忆证据。
- 对公开测试题硬编码、使用金标、泄漏或重建评测数据。
- 跨用户/样本共享记忆，或使用提示词注入操纵 Answer/Eval。
- 人工实时答题、结果操纵、恶意占用评测资源或提交大量近似版本。
- 上传 Eval Key、Memory System Key、模型 API Key 或其他凭证。

## 提交流程

1. 完成并验证 Hindsight AML 基线。
2. 固定 commit，创建 release/tag，公开 GitHub 仓库。
3. 补齐 README、Docker、LICENSE、ATTRIBUTION 和方法说明。
4. 截止前提交 Evaluation Access Request 和完整材料。
5. 配合平台部署、Smoke、问题修复、Full 和结果复核。

首个合格版本的标准是：主办方 clone 仓库、配置密钥、运行一条 Compose 命令后，Add 写入的内容能立即被
Search 检索，且不同用户严格隔离。原始消息检索、时间重排、去重和多跳搜索只能在该基线完成后优化。
