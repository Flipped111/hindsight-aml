# AML 稳定基线与检索优化计划

> 更新日期：2026-08-07（UTC+8）
> 本文记录当前可提交版本、后续检索优化顺序、A/B 评测方法和新版本发布门槛。赛事契约、版本冻结和替换政策
> 仍以 Agent Memory Leaderboard 官方最新文档及审核回复为准。

## 1. 当前结论

当前稳定版本已经公开发布，可以继续作为官方 Smoke/Full 的部署基线：

```text
repository: https://github.com/Flipped111/hindsight-aml
local branch: aml-optimize
release tag: aml-v0.2.1
commit: 95e50728058f23e2238893228b7b35775de9f533
upstream: Hindsight 0.8.6
upstream commit: 436bc7c156f1c94714ea1f757bfc930ab89f883b
```

`aml-v0.2.1` 是接口和部署稳定基线，不包含会改变检索排序的实验性优化。后续工作应从该版本创建独立候选
分支，先进行同环境 A/B 对比，不能直接覆盖现有 tag，也不能在没有指标支持时要求平台频繁替换版本。

如果平台已经开始或冻结 `aml-v0.2.1` 的 Full 评测，应保留该版本完成当前任务；新的检索优化用于下一次
候选提交。

## 2. 当前系统职责

项目在 Hindsight 外部提供独立 AML Adapter：

```text
AML POST /add
    -> 请求校验、user_id 隔离、稳定文档 ID、SQLite 幂等
    -> Hindsight retain（同步完成）
    -> gpt-4o-mini 事实抽取
    -> 本地 embedding、reranker、索引和 pg0 持久化

AML POST /search
    -> user_id 映射到唯一 Hindsight Bank
    -> Hindsight recall（budget=mid）
    -> 按 scores.final 降序并截断 top_k
    -> 只返回真实记忆证据

AML GET /health
    -> 检查 Adapter、SQLite、Hindsight 和数据库
```

平台负责统一 Answer 和 Eval。任何后续优化都不得调用 `reflect` 生成答案，不得利用 `options` 操纵回答，
不得返回无法追溯到真实记忆的内容。

## 3. 已完成能力

### 3.1 AML 契约

- Add 接收 `request_id`、`messages`、`user_id`、`session_id`。
- `messages[].timestamp` 可选；缺失时使用 Hindsight 的 `unset`，不伪造事件时间。
- Add 仅在同步 retain、抽取、向量和索引全部完成后返回 HTTP 200。
- Add 成功响应返回 `success=true`，并原样返回三个 ID。
- Search 接收 `query`、可选 `options`、`user_id` 和 `top_k`。
- Search 始终返回顶层 `data`；无结果时返回 `{"data":[]}`。
- Search 只调用 recall，按 `scores.final` 排序，结果数不超过 `top_k`。
- Health 无需鉴权，只有 Adapter、SQLite、Hindsight 和数据库都正常时才返回 2xx。

### 3.2 隔离、幂等和错误处理

- `sha256(user_id)` 是唯一 Bank 映射，不同用户严格隔离。
- `sha256(request_id:index)` 是稳定消息文档 ID。
- SQLite 在 retain 前原子登记 processing，成功后才登记 completed。
- 相同请求重试直接成功；冲突请求返回 409；失败和失联租约可以安全重试。
- 业务错误格式为 `{"detail":{"reason":"..."}}`。
- 字段校验使用结构化 HTTP 422；依赖失败返回 502；幂等等待超时返回 503。

### 3.3 评测和验证

- `tools/aml_eval.py` 已实现真实 AML `/add`、`/search` 评测链路。
- runner 记录 Add/Search 成功率、P50/P95/P99、证据 ID、内容、score 和排序。
- runner 分别计算答案词和标注来源证据的 Hit@1、Hit@5、Hit@10、Hit@100 和 MRR。
- 已增加 LoCoMo manifest 转换、报告比较、raw 索引种子和共享 Hindsight 状态的搜索重放工具。
- 当前自动化测试共 59 项，覆盖契约、隔离、幂等、错误路径、混合检索、raw 配额、时间重排和评测工具。
- 已完成真实 Docker Compose、`gpt-4o-mini` Add/Search、全新 clone 和持久化验证。
- `.env`、API Key、SQLite 数据和运行 volume 不进入 Git。

## 4. 当前基线限制

`aml-v0.2.1` 仍完全依赖 Hindsight 抽取事实和 recall 排序，尚未实现：

1. 原始消息与抽取事实的混合检索。
2. Adapter 层的时间敏感重排。
3. 结果去重和来源多样化。
4. 多跳证据检索。

这些是下一阶段候选能力，不能在 README 或提交材料中提前写成已实现。

最终 `aml-v0.3.0` 已经实现第 1 项、受限的第 2 项，以及第 3 项中的精确去重和单文档结果上限；这些能力
不属于冻结的 `aml-v0.2.1`。多跳检索和基于相似度的语义去重仍未实现。

## 5. 优化和发布原则

每项优化必须遵循以下规则：

1. 从 `aml-v0.2.1` 创建独立候选分支。
2. 一次只引入一个主要排序变量，避免无法判断收益来源。
3. 基线和候选使用相同数据、模型、预算、机器和并发配置；搜索排序实验必须共享同一份 Hindsight 记忆
   状态，避免两次 LLM 抽取波动污染比较。
4. 每次运行保存完整 runner 报告，不只记录一个汇总分数。
5. 同时比较检索质量、延迟、错误率、隔离性和资源占用。
6. 用户隔离泄漏数必须为 0，Add 成功率和幂等行为不得回退。
7. 只有质量明确提升且延迟可控，才创建并发布新版本。
8. 已发布 tag 永不移动；失败实验只保留在本地或候选分支。
9. Full 版本冻结后不因成绩不理想临时替换。

## 6. A/B 评测方法

### 6.1 固定条件

基线和候选都必须固定：

- Add/Search 使用 `openai` 和 `gpt-4o-mini`。
- embedding 和 reranker 使用相同本地模型。
- Hindsight recall 使用相同 `budget=mid`。
- 相同 `top_k`、数据切分、消息顺序和并发数。
- 使用独立 Docker volume 或全新的 `user_id`，避免历史数据污染。

### 6.2 必须记录的指标

- Add 成功率、同步耗时和失败重试结果。
- Search 成功率、P50、P95、P99 和响应大小。
- Hit@1、Hit@5、Hit@10、Hit@100。
- MRR 和正确证据的首个排名。
- 前 K 条精确重复率、近义重复率和来源 session/document 覆盖数。
- 时间更新题中最新有效事实的优先率。
- 跨用户泄漏数，目标必须为 0。
- 模型调用次数、总运行时间、CPU 和内存峰值。

### 6.3 评测规模

```text
阶段 A：1 个对话、5 个问题，验证链路和报告格式
阶段 B：1 个完整对话、全部有效问题，定位回归
阶段 C：LoCoMo 10 个对话，比较总体 Hit@K 和 MRR
阶段 D：LongMemEval 分类别样本，重点验证时间更新和多 session 问题
```

不能把单个问题、单次运行或同模型 Judge 给出的高分当作发布依据。Answer/Eval 结果只能作为辅助指标，检索
候选必须先通过确定性证据检查。

## 7. 第一优先级：原始消息混合检索（候选已实现，阶段 A/B 已完成）

### 目标

补足 Hindsight 事实抽取可能遗漏的姓名、精确日期、数字、否定信息和原话细节。预计这是下一阶段收益最大的
检索改动。

### 实现边界

- Add 继续保存完整原始消息、角色、session 和事件时间。
- 原始消息使用现有稳定来源 ID，不按搜索请求生成随机 ID。
- 原始消息索引必须按 `user_id` 隔离并持久化，Docker 重启后仍可检索。
- Search 将 Hindsight 抽取事实与原始消息候选合并，但仍只返回真实证据。
- 不调用 Answer、Judge 或 `reflect`，不使用 `options` 改写查询。
- 同一原始消息及其抽取改写不能占满前排结果。

### 首轮测试集

- 精确姓名和身份。
- 日期、年份、金额和数量。
- 否定表达和状态变化。
- 用户原话和引用。
- 抽取事实遗漏但原始消息明确存在的内容。

### 验收

- 姓名、日期、数字和引用类问题的 Hit@K 或 MRR 明确提升。
- 当前核心事实问题的正确证据首个排名不下降。
- 不新增跨用户泄漏、重复写入或重启丢失。
- Search P95 增幅处于预先记录的可接受范围。

### 2026-08-07 共享状态阶段 A 结果

在远程 GPU 机器上固定一次 Add/LLM 抽取，确认基线进程版本为 `0.2.1`、候选为 `0.3.0-dev`，两者搜索
同一份 Hindsight 记忆。候选仅额外加载确定性的 raw SQLite 索引。5 个 LoCoMo 问题结果如下：

```text
答案首个命中排名：5/5 不变
答案 MRR：0.65 -> 0.65
Evidence Hit@5：0.0 -> 0.2
Evidence Hit@10：0.0 -> 0.6
Evidence Hit@100：0.0 -> 1.0
Evidence MRR：0.0 -> 0.0965675
```

强 raw 使用 `0.95` RRF 权重，保留前四个 fact；只有说话人/姓名匹配的弱 raw 使用 `0.55`，在较深位置
补足语义改写无法词法命中的原始来源。本结果证明阶段 A 无答案排名回退并提高来源证据覆盖，但样本仅 5 题，
不能作为正式发布或官方榜单增分证明。延迟也不从该顺序重放得出结论，因为候选使用了已经预热的共享服务。

### 2026-08-07 共享状态阶段 B 结果

阶段 B 使用一个完整 LoCoMo 对话，包含 19 个 Add、419 条消息和 197 个有效问题。Add 只执行一次，基线和
候选确认分别为 `0.2.1` 和 `0.3.0-dev`，自动 consolidation 关闭；后续排序调参复用同一份 Hindsight 状态和
同一份候选 raw SQLite 数据库，不重新执行随机 LLM 抽取。最终重放结果如下：

```text
Add 成功：19/19
基线/候选 Search 成功：197/197
答案 Hit@1：0.324873 -> 0.324873（不变）
答案 Hit@5：0.426396 -> 0.436548（+0.010152）
答案 Hit@10：0.472081 -> 0.497462（+0.025381）
答案 Hit@100：0.573604 -> 0.578680（+0.005076）
答案 MRR：0.376164 -> 0.378919（+0.002755）
候选 Evidence Hit@5/10/100：0.304569 / 0.472081 / 0.532995
候选 Evidence MRR：0.088851
Top100 新命中/丢失：1 / 0
```

最初不限制 raw 数量时，有 3 个基线深层事实被挤出 Top100。最终策略在 fact 仍可用时最多接纳 4 条 raw，
fact 不足以填满 `top_k` 时再用延后的 raw 回填；这恢复了全部 3 个深层事实，并保留 1 个新答案命中。分类
结果中 category 1 和 category 3 的 Hit@5 仍有小样本回退，因此不能继续围绕这一个对话调参；下一步必须
使用 Stage C 留出样本判断这是随机波动还是系统性问题。顺序重放中候选使用预热服务，延迟只作诊断记录，
不作为性能提升声明。

## 8. 第二优先级：时间敏感重排（候选已实现，扩大样本待 A/B）

### 目标

仅对“现在”“目前”“最新”“后来”“搬家后”等有可靠方向的时间意图，提高最新有效事实的排序，避免对普通历史问题
统一施加新近偏置。

### 方法

- 先判断查询是否包含当前状态、先后比较或时间范围意图。
- 只在时间意图明确时施加有限的事件时间加权。
- 相关度仍是主分数，时间只能作为受限加成或冲突事实的次级排序依据。
- 严格区分事件时间和持久化时间；缺少事件时间时不伪造当前时间。
- 无时间意图的查询保持基线排序。

### 当前候选实现

- 识别当前/最新、以前/早先、明确年份、年份范围、某年以前和某年以后意图。
- 裸“最近/recently”在没有查询时间锚点时不触发重排，避免把对话当时的相对时间误解释成全局最新。
- 在 RRF 基础分上施加最大 8% 的事件时间加成，不使用持久化时间替代事件时间。
- 缺少或无法解析事件时间的证据不获得时间加成。
- 普通查询的乘数固定为 `1.0`，保持原 RRF 分数和顺序。
- 四位数字只有位于明确年份上下文时才按年份处理，避免把订单号、验证码等数字误判为时间。

### 必须覆盖的反例

```text
2025：用户住在上海
2026：用户搬到东京
问题一：用户现在住在哪里？       应优先东京
问题二：用户 2025 年住在哪里？   应优先上海
```

### 验收

- 当前状态和知识更新题的最新事实优先率提升。
- 指定历史时间的问题不被新事实覆盖。
- 无时间意图问题的 Hit@K 和 MRR 不出现系统性下降。
- 时间判断和重排开销不造成明显 P95/P99 回退。

## 9. 第三优先级：去重和来源多样化（精确去重已实现，语义去重待评测）

### 目标

防止前十条被同一事实的多个近义改写占满，让平台 Answer 模型获得更多互补证据。

### 方法

- 先按稳定来源 ID 和规范化文本做确定性精确去重。
- 保留重复组中相关度最高的结果。
- 对高度相似文本做受限的语义去重，阈值必须通过 A/B 确定。
- 在不降低正确证据首位排名的前提下增加 session/document 来源覆盖。
- 只有精确去重不足时才考虑轻量 MMR。

### 验收

- 前 5、前 10、前 100 的重复率下降。
- 正确证据的首个排名不下降。
- 多 session 和多证据问题的来源覆盖数提升。
- 不因过度去重删除互相矛盾但对时间推理有用的事实。

## 10. 第四优先级：多跳检索

### 目标

处理需要组合多个 session、实体关系或因果链才能回答的问题。该阶段收益不确定且延迟风险最高，因此最后
实施。

### 约束

- 多跳只扩大或重排真实检索证据，禁止直接生成最终答案。
- 禁止调用 `reflect`，禁止返回模型生成的推断文本作为记忆。
- 每一跳都使用同一 `user_id` 对应的 Bank 和原始消息范围。
- 限制最大跳数、每跳候选数、总候选数和整体超时。
- 简单问题应保留单跳快速路径。

### 验收

- 多 session、因果和实体关联问题的证据覆盖率提升。
- 简单单跳问题的 Hit@K 不下降，延迟没有显著增加。
- 最终每条结果都能追溯到稳定来源 ID 和真实存储内容。
- 在并发 Search 下无持续排队、内存积压或超时增长。

## 11. 新版本发布门槛

候选版本必须同时满足：

```text
[x] 59 项当前测试全部通过，并已补充混合检索、raw 配额、时间重排和共享状态 A/B 回归测试
[x] Ruff、format、ty、compileall 通过
[x] Docker Compose 从零构建和健康检查通过
[x] 真实 gpt-4o-mini Add 后可以立即 Search
[x] user A / user B 隔离回归测试泄漏数为 0
[x] 幂等重试、冲突、失败恢复和重启持久化回归测试无回退
[x] 官方 Add/Search/Health 字段及错误格式回归测试不变
[x] Search 不调用 reflect，不生成答案，不使用 options 操纵检索
[x] 阶段 A 基线与候选共享状态 A/B 报告完整保存
[x] 阶段 B 总体 Hit@5/10/100 和 MRR 提升，Top100 无丢失命中
[ ] 阶段 C 验证总体收益，并确认 category 1/3 回退不具有系统性
[ ] P95/P99、错误率和资源占用处于可接受范围
[x] 发布文件范围的密钥特征扫描无命中，`.env`、运行数据库和评测数据不进入 Git
[ ] 从公开 tag 全新 clone 后可以复现
```

由于最终提交截止时间不允许继续 Stage C，本次以已完成的 Stage A/B、59 项测试和总体 Hit@K/MRR 提升为
依据发布 `aml-v0.3.0`。未完成的 Stage C、分类回退确认和独立并发性能验证作为明确接受的发布风险保留，
不得在提交说明中宣称这些项目已经通过。

## 12. 立即执行的下一步

```text
1. 保持 aml-v0.2.1 tag 和旧数据卷冻结，不修改或复用
2. aml-v0.3.0 已完成原始消息索引、混合 RRF、四条 primary raw 配额、精确去重和受限时间重排
3. 本地继续运行 59 项确定性测试和小型合成样例，不下载超出本机容量的大型数据集
4. 阶段 A 已完成：答案排名无回退，原始来源证据覆盖提升
5. 阶段 B 已完成：总体 Hit@5/10/100 和 MRR 提升，Top100 新命中 1、丢失 0
6. 因最终截止时间，接受 Stage C、category 1/3 和独立性能验证尚未完成的风险并发布 aml-v0.3.0
7. 提交说明只声明已验证的 Stage A/B 总体结果，不声明官方榜单增分或延迟提升
8. 发布后如仍允许迭代，再使用留出样本检查分类回退，不移动已发布 tag
```

最终版冻结当前 Stage A/B 已验证实现，不再针对单个对话继续调参。`aml-v0.3.0` 发布后不得移动 tag；若平台
仍允许后续迭代，Stage C、category 1/3、时间更新题和独立性能测试进入下一版本，而不是回写当前发布。
